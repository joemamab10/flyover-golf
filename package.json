{
  "name": "flyover-golf",
  "private": true,
  "type": "module",
  "scripts": {"build":"node scripts/build.mjs", "test":"node --test tests/*.test.js && npm test --prefix api", "db:generate":"drizzle-kit generate"},
  "devDependencies": {"esbuild":"0.25.12", "drizzle-kit":"0.31.10", "drizzle-orm":"0.45.2"}
}
