# Shared Contracts

These files define the data contracts shared between Flyover UI and API.

The most important contract right now is `tee-time.schema.json`, which is the normalized tee-time shape every provider adapter must produce.
